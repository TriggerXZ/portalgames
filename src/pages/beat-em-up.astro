---
import Layout from '../layouts/Layout.astro';
import JuegosItem from '../components/JuegosItem.astro';
import HeroJuegos from '../components/HeroJuegos.astro';

import { GENRES } from '../../juegos.json';
---
<Layout title="Beat'em Up" description="Beat'em Up">
    <main class="pt-12">
        <HeroJuegos title="Beat'em Up" description="Los clasicos de 'Yo contra el barrio'"/>
        <section class="flex flex-col items-center justify-center">
            <article class="grid grid-cols-1 md:grid-cols-2 gap-3">
                {
                    GENRES.map(genre => {
                        if (genre.genre === 'Beat\'em Up' && genre.JUEGOS) {
                            return genre.JUEGOS.map(juego => (
                                <JuegosItem {...juego} />
                            ))
                        }
                    })
                }
            </article>
        </section>
    </main>
</Layout>


